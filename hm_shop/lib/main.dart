import 'package:flutter/material.dart';
import 'package:hm_shop/routes/index.dart';

void main(List<String> args) {
  // 启动应用
  // runApp()
  // runApp 中返回的 widget 组件是从 路由中导出的组件
  runApp(getRootWidget());
}